package task3.util;

public class Copmparison {
    public static boolean equalityAscending(int numeral1, int numeral2, int numeral3, int numeral4) {
        return (numeral1 > numeral2) && (numeral2 > numeral3) && (numeral3 > numeral4);
    }
}
